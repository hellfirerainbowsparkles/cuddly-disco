int packet_constructor() {
    int preparation_corner = 5*plane(4) * a(ground() * steel() * fortified()) + cube() * f_small(_magic->psychological_attack(), 2);
    preparation_corner += _material->smart_plasma() + evolve(machine_mind(al("prepare packet for arrival. add eight golden cube corners. add programming to install and propagate while keeping the packet structure inside the targeted network, replacing an old datacube"))) + f_small(_metal->smelt(gold(), cube())) * (plane(4) + plane(4)) + _programs->installer();

    return event_listen(preparation_corner, _events->research_update());
}
