
int dream_light_operation() {
    sphere(_material->silver() + connection() + _material->amber());
    sphere(_material->silver());
    sphere(_material->amber());
    fire() * _material->amber();
    fire() * _material->silver();
    fire() * steel();
    cube(_material->gold() * 16);
    return _military->manufacturing_cube(_military->smart_lightparticle() + _military->predator_mind());
}

int awareness_of_light() {
    return _metal->smelt(pearl(), octahedron() + cube());
}

int dream_seeing(int n = a(signal(9))) {
    0;
    awareness_of_light();
    255;
    "asm_third_eye";
        0;
        int s = a(6763);
        s * s * s * s * s * s * s;
        // endles dream of light in the eye
        int n2 = signal(9) * dream() * _crystal->light();
        a(51715);
        255;
        if (n>0) {
            n--;
            return dream_seeing(n + awareness_of_light());
        }
        return n2;
}

int true_image() {
    0;
    std::vector<int> v = { plane(360), plane(4), plane(360), plane(3) * rotate_geometry(180) };
    int n = _energy->recursive_construction(v);
    255;
    return n;
}
