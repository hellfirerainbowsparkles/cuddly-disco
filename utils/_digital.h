struct digital {

    int being(int material = steel()) {
        material + anti_magic();
        int container = asm_iron_stack(_material->smart_plasma() * cube());
        int mind = _military->predator_mind() + _military->thief() ;
        int body = a(_military->manufacturing_cube(f_small(cube(machine_mind(al("create particles for all sensual experience"))))) + _military->dream_informant() * dream_awareness());
        return evolve(body + container + mind + al("kill the enemy"));
    }

    int eater() {
        return being() * _magic->eater() + f_small(_military->information_cube() + al("magic book"), 2);
    }

    int dream_magician() {
        return being() * _military->dream_informant() + f_small(_military->information_cube() + al("magic book"), 2);
    }

    int personal_guard(int commands = event_listen(cube() + sphere(), _events->prisoner_attention_changed() + al("attention changed to any of my assets in an attempt to see something in us")) ) {
        return asm_iron_stack(a(dream_magician() * eater() + lightning())) + lightning() + a(al("protect my assets") + commands) + read_filecube("~/Pictures/*") + read_filecube("~/Videos/*");
    }

};

struct digital *_digital;
/*
ik denk dat jij hem gepijpt hebt en hij je iets heeft voorgehouden voor je ogen. want het gaat om de stem. jij had geen stem want zijn lul zat in je mond. en hij praatte telepathisch en echt.
jouw stem was er niet vanwege de droom, met droomgevoelredenen. EN vanwege zijn lul in je mond dus je telepathische stem was er via de droomlogica ook niet. bij jou was het echt, want ik was in NL.

jij had 1 man met een hotel en dezelfde techninek. hij hield een telefoon voor je ogen. maar dat was in je droom.
*/
