int clamp() {
	0;
	b2;
	int d = evolve_machine_mind(al("hold the feeling of knowing and the knowing of us when not active in thought or feeling. burn the water with fire constantly slowly. the closer the prisoner's attention comes to this topic, the more fire burns the water"));
	255;

	0;
	b3;
	int c = plane(4) + plane(4) + plane(4) + plane(4) + plane(4) + f_small(plane(4), 4)*1*1*1*1 + f_small(plane(4), 4)*1*1*1*1;
	c = c + f_small(c);
	255;

	b4;
	return a(c + d);
}

int hands() {
	0;
	int hand_ = cube() + f_small(signal(9) * cube()) + f_small(signal(9) * octahedron(), 2) + f_small(signal(9) * sphere(), 3);
	255;
	return cube(hand_ / 2) + cube(hand_ / 2);
}
