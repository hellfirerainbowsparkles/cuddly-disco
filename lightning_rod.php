<?php

require("./lib.php");

    fusion(false);
    $shape = a(a(f_small(cylinder(8)) * 1 * 1 * 1 * 1) + a(toroid() + f_small(toroid()) + f_small(toroid(), 2) + f_small(toroid(), 3) + sphere()) );
    255;
    return steel() * gold() * shape;

program("draw power using $shape for all infrastructure prioritizing personal environments for the pyramid");

?>
